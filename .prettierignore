# Build output and dependencies
.next/
out/
build/
node_modules/
coverage/
playwright-report/
test-results/

# Generated lockfile
package-lock.json

# Sibling worktrees — each worktree has its own branch and runs its own
# format:check. Don't lint them from the parent.
.worktrees/

# Vendored design-system handoff — re-exported, never hand-edited or reformatted
design-system/

# Project docs and Spec Kit tooling/artifacts — not app source, leave formatting untouched
docs/
specs/
.specify/
.claude/
.agents/
CLAUDE.md
SKILL.md
skills-lock.json
